export enum Page {
  Game = 'game',
  Games = 'games',
  Levels = 'levels',
  Menu = 'menu',
  Settings = 'settings',
}

export enum Theme {
  Dark = 'dark',
  Light = 'light',
  System = 'system',
}

export enum Level {
  Easy = 'easy',
  Extreme = 'extreme',
  Hard = 'hard',
  Medium = 'medium',
}

export const Levels = [Level.Easy, Level.Medium, Level.Hard, Level.Extreme]
